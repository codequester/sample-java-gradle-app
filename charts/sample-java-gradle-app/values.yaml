# Default values for sample-java-gradle-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: docker.io/shankar29/sample-java-gradle-app
  pullPolicy: IfNotPresent
  tag: "latest"


service:
  type: ClusterIP
  port: 8081

